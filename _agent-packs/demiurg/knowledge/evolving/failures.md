# Failures — что не сработало и почему

Append-only журнал ошибок. Перед каждой сборкой — `grep` на ключевые слова, не повторяй грабли.

```
## YYYY-MM-DD: Что пошло не так

**What:** какая ошибка
**Root cause:** настоящая причина (не симптом)
**Rule:** как избежать в следующий раз
```
