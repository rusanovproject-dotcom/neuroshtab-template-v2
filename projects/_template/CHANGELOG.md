# CHANGELOG

Лог структурных изменений в проекте. Не для каждой правки — только для того что **меняет архитектуру**: новые файлы выводов, переименования, перенос разделов, смена эпох бизнеса.

Формат: `## YYYY-MM-DD — что изменилось / почему`. Append only.

---

## {{TODAY}} — проект инициализирован

Создана базовая структура проекта на основе шаблона `_template/`. Все файлы выводов (audience/, product/, brand/, funnel/) ожидают наполнения экспертом.
